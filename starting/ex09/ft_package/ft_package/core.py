def count_in_list(lst, item):
    """
    Counts the occurrences of `item` in `lst`.

    Args:
        lst (list): The list to search in.
        item: The item to count.

    Returns:
        int: The number of times `item` appears in `lst`.
    """
    return lst.count(item)
